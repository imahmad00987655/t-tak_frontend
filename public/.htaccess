<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Keep real files/directories as-is.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA fallback: send all app routes to React entry.
  RewriteRule ^ index.html [L]
</IfModule>
